server:
  port: 8081

spring:
  application:
    name: PRODUCT-SERVICE

eureka:
  instance:
    hostname: localhost

#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka/
#  instance:
#    hostname: localhost
